/* imports */
import DatabaseController from '../../../../Database/controllers/DatabaseController.js'

/**
 * 
 * @param { object } options 
 * @returns { object }
 */
export default function closeFlag(options) {
  return {}
}